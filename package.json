{
  "name": "nexflow-inventory-root",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "postinstall": "cd backend && npm install",
    "start": "cd backend && node server.js",
    "build": "echo 'No build needed for backend'"
  }
}
